apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels: 
    app: backend
spec:
  type: NodePort      
  selector:
    app: backend
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30800  