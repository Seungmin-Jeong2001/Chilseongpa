# ==============================================================================
# [docker/handlers/main.yml]
# ==============================================================================
- name: Restart Docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
